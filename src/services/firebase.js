import * as firebase from 'firebase';
import firestore from 'firebase/firestore';

const config = {
	"Copy config in firebase website integration"
};

firebase.initializeApp(config);

export default firebase;